# Example API credentials for local development.
# Copy this file to .env and replace the placeholder values with real secrets.

google_key="replace-with-your-google-key"
stripe_key="replace-with-your-stripe-key"
